// merge.h
#ifndef MERGE_H
#define MERGE_H

#include <vector>

// Función para ordenar un vector usando el algoritmo de mezcla
void mergeSort(vector<int>& arr, int left, int right);

void tiempo_merge(vector<int> dataset);

#endif // MERGE_H
