#ifndef DATASET_H
#define DATASET_H
#include <vector>
using namespace std;

// Declaración de la función que genera un dataset de enteros
vector<int> generar_dataset(int size, int max_value);

#endif // DATASET_H
